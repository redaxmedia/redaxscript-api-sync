#!/usr/bin/env php
<?php
namespace Sync;

use Redaxscript\Config;
use Redaxscript\Language;

/* bootstrap */

include('includes' . DIRECTORY_SEPARATOR . 'bootstrap.php');

/* sync */

$sync = new Sync(Language::getInstance(), Config::getInstance());
$sync->run();
